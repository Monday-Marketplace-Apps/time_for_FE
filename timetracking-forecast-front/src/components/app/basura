import TimetrackingForecast from "../../pages/app/TimetrackingForecast";

TimetrackingForecast


    const datosGraficoPrueba = datos[0]['columna1']
   
    const nombrePrueba = nombre[0]
    
    datosGrafico.map((dato) => {
      
      const nombredatodentromap = Object.keys(dato)[0]
      console.log('nombredatodentromap', nombredatodentromap)

      
      
      if (nombredatodentromap === personName){
        const graf=[]
        console.log('entro en if imprimo dato',dato)
        console.log('dato[Object.values(dato)]', Object.values(dato)[0]['datoreal']['diario'])
        const mapeoDatorealDiario = Object.values(dato)[0]['datoreal']['diario']
        
     

        console.log('estoy en el map timetracking')

        mapeoDatorealDiario.map((hora) => {
          console.log('dentro de map graf', hora);
          
          graf.push({ 'fecha': Object.keys(hora), 'horas_h': Object.values(hora) })
          console.log('pasada map graf', graf)
        
          console.log('graf fuera map', graf)
          setDatosGrafico(graf)}
          )
        }